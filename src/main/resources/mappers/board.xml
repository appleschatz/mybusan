<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

	<select id="list" resultType="java.util.Map">
		SELECT ce_sequence,ce_name,ce_content,ce_review,ce_filename
		FROM ac_certification
		ORDER BY ce_sequence
	</select>
	
	<insert id="insert" parameterType="java.util.Map">
		INSERT INTO ac_certification(ce_name,ce_content,ce_review,ce_filename)
		VALUES(#{ce_name}, #{ce_content} , #{ce_review} , #{ce_filename})
	</insert>
	
	<select id="search" resultType="java.util.Map">
		SELECT ce_sequence, ce_name, ce_content, ce_review , ce_filename
		FROM ac_certification
		WHERE ce_name LIKE #{ce_name}
		ORDER BY ce_name
	</select>
	
	<select id="detail" resultType="java.util.Map">
		SELECT ce_sequence, ce_name, ce_content, ce_review , ce_filename
		FROM ac_certification
		WHERE ce_sequence = #{ce_sequence}
	</select>
</mapper>

